#!/bin/bash

# Debugging settings:
#####################
# exit immediately upon any error, output each line as it is executed
set -ex -o pipefail

mkdir -p results
mkdir -p intermediates

cwltool --debug --leave-tmpdir --outdir results --cachedir intermediates ${@:1} 2>&1 | tee log.txt
